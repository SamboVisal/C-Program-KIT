#include<stdio.h>
int i,sum=0;
main(){
/*	
	c=getc(stdin);
	fflush(stdin);
	d=getc(stdin);
	fflush(stdin);
	putc(c,stdout);
	putc(d,stdout);
	c=getchar();
	fflush(stdin);
	putchar(c);
*/
for(i=20;i>10;i--){
	
//	sum=sum+i;
}		printf("%d",i);
}
